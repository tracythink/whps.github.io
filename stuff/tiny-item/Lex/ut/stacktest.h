void stacktest() 
{	/*
	int i, a = 1, b = 2, c = 3, d = 4;
	struct stack * s = newstack();
	void * p;
	for (i = 0; i < 1000; i++) {
		push(s, Char(i));
	}
	//p = pop(s);	
	//pint(pop(s));
	//pint(pop(s));
	//printf("%p \n%p", s->top, s->p);
	//pint(pop(s));
	//stackdump(s, pint);*/
}