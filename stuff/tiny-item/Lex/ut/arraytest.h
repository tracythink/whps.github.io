void arraytest()
{/*
	int i;
	struct array * n = newarray();
	
	for (i = 0; i < 1000; i++) {
		assign(n, i, Char(i));
	}
	assign(n, 2, Char(4));
	assign(n, 3, Char(1));
	printf("%p", index(n, 3));
	printf("%p", index(n, 1) == NULL);
	pint(index(n, 1));
	arraydump(n, pint);*/
}